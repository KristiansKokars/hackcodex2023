// NOTE: We would pass errors with codes so we can localize them in the web app correctly, but for the Hackathon, we are passing them as simple string messages
export interface SimpleErrorMessage {
	message: string;
}
